-- Step 3: Drop the database if needed
-- Be cautious! This is irreversible.
USE `UniversityDB`;

-- The IF EXISTS clause prevents errors if the database doesn't exist
DROP DATABASE IF EXISTS `UniversityDB`;
