-- Step 1: Create a database called UniversityDB
CREATE DATABASE `UniversityDB`;

-- Step 2: Use the newly created database
USE `UniversityDB`;

-- Verification (Optional): Check if the database is selected
SELECT DATABASE();

